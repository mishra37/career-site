# Optional: Set this for enhanced AI-powered resume matching using OpenAI embeddings.
# If not set, the app will use keyword-based matching (still works great!).
OPENAI_API_KEY=your_openai_api_key_here
